@model int[]

@{for (int i = 0; i < Model.Length; i++)
    {
        @Model[i]
    }
}

@{for (int i = 0; i < Model.Length; i++)
    {
       @(Model[i] + " ")
    }
}
@{ foreach (var i in Model)
    {
        @i.ToString()
    }

}


<ul>
    @{for (int i = 0; i < Model.Length; i++)
        {

  <li>@Model[i]</li>
        }
    }</ul>